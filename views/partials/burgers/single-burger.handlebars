<li>
	{{name}}

	{{!-- <button class="eatIt" data-id="{{id}}" data-eat="{{sleep}}">
		{{#if is_eaten}}SLEEP TIME!{{else}}WAKE UP!{{/if}}
	</button> --}}
    <hr>
	<button class="deleteBtn" data-id="{{id}}"> Yuck, Off the Menu!</button>
</li>
